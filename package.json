{
  "name": "rust-gear-project",
  "private": true,
  "scripts": {
    "prepare": "git config core.hooksPath .githooks",
    "build": "pnpm -r build",
    "packages": "pnpm -r packages",
    "readme": "pnpm -r readme",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^22.15.23",
    "ava": "^6.4.0"
  }
}
