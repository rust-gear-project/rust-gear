{
  "name": "rust-gear-project",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "prepublish": "pnpm -r prepublish",
    "packages": "pnpm -r packages",
    "test": "pnpm -r test"
  }
}
